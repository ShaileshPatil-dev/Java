package video7;
public class test {
    public static void main(String[] args) {
        /*
        we will write the code with the java 
        .java will be the file extention (Human readable)
        this will be source code that we will provide to the compiler and convert it into the .class file known as the byte code , it does not convert it directly in machine code (0/1)
        to run the byte code we need the JVM (Java Virtual Machine) this is the reason java is a platform independent language
        the interpreter compile the byte code line by line
        and convert it to machine code

        */
    }
}
