package video5;

public class intro {
    public static void main(String[] args) {
        /*
        Programs : set of instruction 
        Computer understands 0/1 (Binary Language) to communicate with Computer we use Programming languages
        A programming language allows programmers to communicate instructions to a computer in human readable format.
        eg: Java, C, C++, Python etc.

        Programming languages are defined by their syntax (formay) and semantics (meaning).

        Types of language :
        1. Procedural
        2. Functional
        3. Object Oriented

        A. Static 
        Type checking at compile time
        need to declare the datatype before using vriable
        B. Dynamic
        Type checking at Runtime
        No conversion 
        no need to declare the datatype 

        Memory mangement :
        1. Stack memory
        2. Heap memory

        When we write a = 10;
        here, a is a refrence variable and 10 is an object 
        so the variable , function calls etc it stored in stack memory
        and the object is stored in the heap memory
        and variable 'a' is pointing to the object '10'

        Understand :
        1. more than one refrance variables can point to same object 
        exp : suppose im the Object and my mom calls me son (Ref Variable 1) My brother calls me Shailu (Ref Variable 2) my Teammate calls me insider (Ref vaibable 3) Hypo Gf calls me baby (Ref Variable 4)

        2. if the any one of the ref Variable change the object then that object is going to chnage for every other object 
        exp : suppose my Gf says chanage the color of the hair then that change is going to visible for every other variable

        3. Java has pass by Refrence value

        4. All the objects has no refrence variable that will get deleted by garbage collection automatically in java
        exp : suppose i earned all the things and now i become the monk so i (obeject) dont have the identity as the baby or shailesh (Refrence)
        then i will automatically remove the connection form the world (Get deleted by the garbage collector)
        exp 2 :suppose 
        a = 10 // a is point to 10
        a = 42 // get updated and now a is pointing to 42 
        so the previous value 10 has no ref variable is addressing then the garbage collector(Yamraj) will delete that 10

        
        */
    }
}
